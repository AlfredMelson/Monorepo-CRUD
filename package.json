{
  "name": "insly-monorepo",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "concurrently \"wsrun --parallel start\"",
    "clean": "lerna clean --yes && rimraf node_modules yarn.lock"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/rimraf": "^3.0.2",
    "concurrently": "^7.0.0",
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2",
    "wsrun": "^5.2.4"
  }
}
