{
  "name": "packages",
  "version": "1.0.0",
  "main": "index.ts",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "concurrently \"wsrun --parallel start\"",
    "clean": "lerna clean --yes && rimraf node_modules yarn.lock"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^7.0.0",
    "wsrun": "^5.2.4",
    "@types/rimraf": "^3.0.2",
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2"
  }
}
