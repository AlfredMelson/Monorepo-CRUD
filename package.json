{
  "name": "root",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "concurrently \"wsrun --parallel start\"",
    "clean": "lerna clean --yes && rimraf node_modules yarn.lock"
  },
  "devDependencies": {
    "@types/rimraf": "3.0.2",
    "concurrently": "7.4.0",
    "lerna": "6.0.1",
    "rimraf": "3.0.2",
    "wsrun": "5.2.4"
  }
}

